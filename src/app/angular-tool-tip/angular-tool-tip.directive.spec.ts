import { AngularToolTipDirective } from './angular-tool-tip.directive';

describe('AngularToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
